## Packages
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge Tailwind classes without style conflicts
framer-motion | High-performance animations and page transitions

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["var(--font-sans)"],
  display: ["var(--font-display)"],
}

The app requires dark mode styling to fulfill the "desktop widget/Spotlight" frosted glass aesthetic. I am enforcing dark mode globally in the CSS.
